import 'package:flutter/material.dart';

class CustomerDetailTabletscreen extends StatelessWidget {
  const CustomerDetailTabletscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
